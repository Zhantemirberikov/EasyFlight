<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.easyflight">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EasyFlight"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Главный экран -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Экран входа -->
        <activity android:name=".LoginActivity" />

        <!-- Экран регистрации -->
        <activity android:name=".RegisterActivity" />

        <!-- Экран результатов поиска -->
        <activity android:name=".FlightResultsActivity" />

        <!-- Профиль пользователя -->
        <activity android:name=".UserProfileActivity" />

        <activity android:name=".FlightDetailActivity" />

        <activity android:name=".BookingConfirmationActivity" />

        <activity android:name=".BookingHistoryActivity" />


    </application>
    <uses-permission android:name="android.permission.INTERNET" />

</manifest>
